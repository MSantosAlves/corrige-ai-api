services:
  mongodb:
    image: mongo:7
    container_name: corrige-ai-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: corrige-ai
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
